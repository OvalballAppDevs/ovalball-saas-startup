// No selection yet -- the layout's PositionExplorerClient reads the
// (absent) [positionKey] segment itself via useParams and renders the
// "select a position" prompt. This page contributes no UI of its own.
export default function PositionExplorerCodePage() {
  return null
}
